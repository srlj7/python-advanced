import pandas as pd

def load_data():
    """وظيفة هذه الدالة هي فقط جلب البيانات"""
    data = {
        'email': ['saif@gmail.com', 'ALI@hotmail.com', 'noor@yahoo.com', 'invalid_mail'],
        'score': [85, 90, 120, 88]
    }
    return pd.DataFrame(data)

def clean_data(df):
    """وظيفة هذه الدالة هي التنظيف فقط"""
    # 1. توحيد الإيميلات لأحرف صغيرة
    df['email'] = df['email'].str.lower()
    
    # 2. حذف الدرجات المستحيلة (أكثر من 100)
    df = df[df['score'] <= 100]
    
    return df

def main():
    """هذا هو المدير الذي يشغل المصنع"""
    print("--- 🚀 بدء عملية المعالجة ---")
    
    # 1. جلب المواد الخام
    raw_data = load_data()
    print(f"تم تحميل {len(raw_data)} صفوف.")
    
    # 2. تشغيل آلات التنظيف
    clean_data_df = clean_data(raw_data)
    
    # 3. إخراج المنتج النهائي
    print("--- ✅ البيانات النظيفة ---")
    print(clean_data_df)

# جملة شرطية لضمان أن الملف يعمل فقط عند تشغيله مباشرة
if __name__ == "__main__":
    main()